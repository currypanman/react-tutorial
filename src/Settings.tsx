import React from 'react';

type SettingsProp = {
};

class Settings extends React.Component<SettingsProp> {
  render () {
    return (
      <div>
        This game is not configurable (^_^).
      </div>
    );
  }
}

export { Settings };
